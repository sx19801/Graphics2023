#ifndef REDNOISE_H
#define REDNOISE_H

#include <CanvasTriangle.h>
#include <DrawingWindow.h>
#include <Utils.h>
#include <fstream>
#include <vector>
#include "Triangle.h"
#include "camera.h"
#include "objFuncs.h"
#include "rayTracing.h"

#define WIDTH 320
#define HEIGHT 240
#define LIGHTSOURCE {0.7,0.5,0.99}
#define BLACK (255 << 24) + (0 << 16) + (0 << 8) + 0
#define RED (255 << 24) + (255 << 16) + (0 << 8) + 0




#endif // !REDNOISE_H
